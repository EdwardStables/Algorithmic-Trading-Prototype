#A place to get started
#Use pl.getData([dataset]) to import the dataset called [dataset]. This currently returns the whole dataset as a pandas dataframe
#Use pl.saveData([dataset]) to save the dataset called [dataset]. This currently  requires [dataset] to be a pandas dataframe.
#If the dataset has a new name it will be stored in the 'User Datasets' directory in the platform directory.

import platform as pl
import pandas as pd

#Write something fun!
print("printing from algo")
#this is a new comment